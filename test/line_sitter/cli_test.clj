(ns line-sitter.cli-test
  (:require
   [clojure.test :refer [deftest is testing]]))

;; Placeholder test to verify test infrastructure works.
(deftest placeholder-test
  (testing "cli-test"
    (testing "has a passing test"
      (is true))))
